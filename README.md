# InformaticProject1

| Method        | Best for                   | Notes                     |
|---------------|-----------------------------|----------------------------|
| Download ZIP  | Quick look, no Git          | No updates, no history     |
| Clone         | Development, contributing   | Full Git features          |
| Fork          | Making your own version     | Keeps your changes separate |

Clone the repository

git clone https://github.com/USERNAME/REPOSITORY.git->git clone https://github.com/OInfinity/InformaticProject1.git


#

# Optional

create a new repository on the command line

create a README.md file and write InformaticProject1 within.

echo "# InformaticProject1" >> README.md

git init

git add README.md

git commit -m "first commit"

git branch -M main

git remote add origin https://github.com/OInfinity/InformaticProject1.git

git push -u origin main

Push an existing repository from the command line

git remote add origin https://github.com/OInfinity/InformaticProject1.git

git branch -M main

git push -u origin main

update existing file in the project

git add .

git git commit -m "YOUR COMMIT MESSAGE"

git branch

git push origin yourBranchName
